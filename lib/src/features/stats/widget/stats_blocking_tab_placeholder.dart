import 'package:flutter/material.dart';

class StatsBlockingTabPlaceholder extends StatelessWidget {
  const StatsBlockingTabPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
